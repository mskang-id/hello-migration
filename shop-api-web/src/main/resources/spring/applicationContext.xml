<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
         http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
         http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- persistence: dataSource, sqlMapClient, transactionManager, db init -->
    <import resource="classpath:spring/datasource-context.xml"/>
    <!-- core: service/facade/biz/manager/pg scan + tx advice -->
    <import resource="classpath:spring/service-context.xml"/>
    <import resource="classpath:spring/tx-context.xml"/>
    <!-- batch: scheduler + settlement rollup / point-expiry sweep jobs (root-context beans) -->
    <import resource="classpath:spring/batch-context.xml"/>

    <!-- DAO impls live in the persistence module -->
    <context:component-scan base-package="com.shopmall.dao.impl"/>
</beans>
