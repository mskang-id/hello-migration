<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
  "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="OrderLifecycle">
  <update id="updateStatus" parameterClass="java.util.HashMap">
    UPDATE orders SET status = #status# WHERE order_id = #orderId# AND status = #fromStatus#
  </update>
  <select id="findStatus" parameterClass="long" resultClass="int">
    SELECT status FROM orders WHERE order_id = #orderId#
  </select>
</sqlMap>
